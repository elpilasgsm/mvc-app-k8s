apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mvnchart.fullname" . }}-test-connection"
  labels:
{{ include "mvnchart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mvnchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
